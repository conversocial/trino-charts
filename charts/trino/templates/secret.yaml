{{- if eq .Values.server.config.authenticationType "PASSWORD" }}
{{- if .Values.server.config.useDefaultAuthenticationSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: trino-password-authentication
  labels:
    {{- include "trino.labels" . | nindent 4 }}
data:
  password.db: {{ .Values.auth.passwordAuth | b64enc }}
{{- end }}
{{- end }}
{{- range .Values.additionalSecrets }}
---
{{- tpl . $ | nindent 0 }}
{{- end }}